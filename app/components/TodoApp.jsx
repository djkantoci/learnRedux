var React = require('react');

var TodoApp = React.createClass({
    render: function () {
        return (
            <div>
                todo app
            </div>
        )
    }
});

module.exports = TodoApp;
